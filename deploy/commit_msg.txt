feat: session detail view + session ID search

- Sessions tab: search by session ID (partial match, debounced)
- Sessions tab: click any row to open detail view
- New route /session/:id renders SessionDetailView
- SessionDetailView: attribution card, session meta card, full journey timeline
- admin-api: GET /sessions/:id returns session + all events ordered by time
- admin-api: GET /sessions supports ?session_id= filter (ILIKE partial match)
