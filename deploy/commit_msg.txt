fix: attribute internal-referrer sessions correctly

When the first event's referrer is an internal particleformen.com URL
that itself carries tracking params (nb_klid, _kx, utm_source, fbclid etc.),
parse attribution from those params instead of marking as direct.
Fixes sessions where the user's session TTL expired mid-browse after
arriving via email/ad link, then continued browsing.
