fix: deduplicate A/B test redirect events (same path within 5s, keep later)

- Worker: deduplicateEvents() collapses same-session + same-pathname events
  within 5 seconds, keeping the later URL (variant). Fixes page_count inflation.
- Backfill: same dedup logic applied when regenerating sessions from DB events.
- SessionDetailView: displayEvents computed deduplicates journey for historical data.
- Worker INSERT now uses ON CONFLICT DO NOTHING for safety.
