fix: run backfill after every deploy truncation

Sessions table was being truncated but only rebuilt from new Redis events,
leaving old events orphaned (session showing wrong page count / entry page).

- vps_deploy_all.py: runs backfill after truncation via docker exec
- worker/package.json: add "backfill" script (node dist/backfill.js)
- worker/src/backfill.ts: uses POSTGRES_HOST env var (works in container)
